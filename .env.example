# Local SQLite database (no external credentials needed for this MVP).
# Resolved relative to prisma/schema.prisma → creates prisma/dev.db
DATABASE_URL="file:./dev.db"

# Admin login. Change these for any real deployment.
ADMIN_PASSWORD="tavola"
ADMIN_SESSION_SECRET="change-me-to-a-long-random-string"
